[build]
builder = "NIXPACKS"
buildCommand = "npm install --include=dev && cd client && npm install && npm run build && cd .. && npm run setup"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
